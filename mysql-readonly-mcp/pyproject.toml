[project]
name = "mysql-readonly-mcp"
version = "0.1.0"
description = "Local bounded read-only MySQL MCP over stdio"
requires-python = ">=3.11,<3.13"
dependencies = ["mcp>=1.26,<2", "PyMySQL[rsa]>=1.1.1,<2", "sqlglot>=28,<31"]

[tool.uv]
package = false
